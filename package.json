{
  "name": "clima-conversion",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint .",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "eslint": "^10.0.0",
    "jest": "^30.2.0"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/**/*.js"
    ],
    "coverageThreshold": {
      "global": {
        "lines": 80,
        "branches": 80
      }
    }
  }
}
